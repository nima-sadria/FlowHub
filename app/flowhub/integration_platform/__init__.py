"""FlowHub Integration Platform.

This package is the internal Integration Platform boundary. API routes read
connector registry, connector instances, settings, health, diagnostics, and
Data Layer records through this package instead of calling provider clients.
"""
