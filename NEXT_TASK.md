# Immediate Next Task

Verify the final merged build on the exact release route set and resolve the `/settings/pricing` browser failure only if it is a real integration defect. If the route absence is intentional, document that explicitly and keep the release verdict blocked until the UI contract and browser evidence agree.
