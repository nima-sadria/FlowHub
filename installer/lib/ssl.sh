#!/usr/bin/env bash
# FlowHub - SSL setup
#
# Modes: off / self-signed / letsencrypt / manual
# Writes certificates to FLOWHUB_STORAGE_PATH/ssl/.
# Configures Nginx accordingly.
#
# This standalone helper is inactive. Use the installed `flowhub tls` command
# or the interactive Domain + SSL Setup menu.

echo "lib/ssl.sh is inactive. Use: flowhub tls status"
exit 1
