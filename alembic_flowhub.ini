[alembic]
# FlowHub - Alembic configuration for FlowHub-only migrations
# FLOWHUB migration files: alembic_flowhub/versions/FLOWHUB_NNN_*.py
# A2 migrations: use alembic_a2.ini
script_location = alembic_flowhub
# Prepend the invocation directory (the repo root / container WORKDIR /app) to
# sys.path so env.py can `import app.flowhub...`. The `alembic` console entry point
# does not add the CWD to sys.path on its own. Mirrors alembic.ini.
prepend_sys_path = .
# sqlalchemy.url is set at runtime from FLOWHUB_DATABASE_URL env var

[loggers]
keys = root,sqlalchemy,alembic

[handlers]
keys = console

[formatters]
keys = generic

[logger_root]
level = WARN
handlers = console
qualname =

[logger_sqlalchemy]
level = WARN
handlers =
qualname = sqlalchemy.engine

[logger_alembic]
level = INFO
handlers =
qualname = alembic

[handler_console]
class = StreamHandler
args = (sys.stderr,)
level = NOTSET
formatter = generic

[formatter_generic]
format = %(levelname)-5.5s [%(name)s] %(message)s
datefmt = %H:%M:%S
