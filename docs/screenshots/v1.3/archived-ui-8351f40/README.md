# Archived UI Reference

These screenshots were selectively preserved from archived commit `8351f40`.
They document the final visual review performed on that branch across:

- 13 application screens
- Light and dark themes
- LTR and RTL directions
- A 1440x900 viewport

The images are historical reference material. They are not golden snapshots for
the current application and must not override behavior or remediation already
present on `origin/main` at `0cfc489`.

The archived application source and E2E files were not copied with these images.
Current-main behavior remains authoritative.
